# ==============================================
# 🚀 ERPLab Backend - Configuração de Deploy
# ==============================================
# Este arquivo contém APENAS configurações (sem credenciais)
# Credenciais devem ser definidas como GitHub Secrets
# ==============================================

# Informações da Aplicação
APP_NAME=erplab
GITHUB_REPO=cria-digital/erplab-back
REGISTRY=ghcr.io
REGISTRY_USER=diegosoek

# Imagem Docker
IMAGE_NAME=${REGISTRY}/${REGISTRY_USER}/${APP_NAME}-backend
IMAGE_TAG=latest

# Servidor
APP_PORT=10016
APP_DOMAIN=erplab.paclabs.com.br
HEALTH_ENDPOINT=/api/v1/health
API_DOCS_ENDPOINT=/api/docs

# Banco de dados (servidor)
DB_HOST=172.17.0.1
DB_PORT=5432
DB_NAME=erplab_prod
DB_USERNAME=erplab

# JWT
JWT_EXPIRES_IN=7d
JWT_REFRESH_EXPIRES_IN=30d

# CORS (múltiplos origins separados por vírgula)
CORS_ORIGINS=https://erplab.paclabs.com.br,https://erp-lab-copy.vercel.app

# Email (SMTP)
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_FROM=nao-responder@paclabs.com.br
MAIL_USER=nao-responder@paclabs.com.br

# Frontend
FRONTEND_URL=https://erplab.paclabs.com.br

# Sentry (opcional - deixe vazio se não usar)
SENTRY_DSN=

# Node Environment
NODE_ENV=production

# ==============================================
# 🔐 CREDENCIAIS (definir como GitHub Secrets)
# ==============================================
# Necessários no repositório do backend:
# - GITHUB_TOKEN (automático)
# - INFRA_DEPLOY_TOKEN (personal access token com read:packages e write:packages)
#
# Necessários no repositório infra:
# - CONTABO_HOST (IP do servidor)
# - CONTABO_USER (usuário SSH)
# - CONTABO_PASSWORD (senha SSH)
# - INFRA_DEPLOY_TOKEN (mesmo token acima)
#
# Necessários no .env do servidor (/opt/infra/apps/erplab/.env):
# - DB_PASSWORD (senha do banco)
# - JWT_SECRET (chave secreta JWT)
# - MAIL_PASS (senha do email)
# ==============================================
